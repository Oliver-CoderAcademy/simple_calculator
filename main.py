def my_function(some_parameter):
    return f"{some_parameter}"

print(my_function("some_other_argument"))

# The function definition needs to give a name to the expected inputs. 
# These names are *parameters*

# When the function is called, the inputs that are given are called the *arguments*